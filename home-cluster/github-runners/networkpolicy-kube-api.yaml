apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: github-runner-allow-kube-api
  namespace: github-runners
spec:
  podSelector: {}
  policyTypes:
  - Egress
  egress:
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
    ports:
    - protocol: TCP
      port: 443
