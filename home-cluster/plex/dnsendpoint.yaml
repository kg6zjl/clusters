apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: plex
  namespace: plex
spec:
  endpoints:
  - dnsName: plex.kube.stevearnett.com
    recordTTL: 180
    recordType: CNAME
    targets:
    - kube.stevearnett.com
