apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: security-scanning
  namespace: kube-system
spec:
  endpoints:
  - dnsName: security.kube.stevearnett.com
    recordType: CNAME
    targets:
    - kube.stevearnett.com