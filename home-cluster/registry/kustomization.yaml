apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: registry

resources:
  - dns-egress.yaml
  - namespace.yaml
  - pvc.yaml
  - ingressroute.yaml
  - deployment.yaml
  - service.yaml
  - external-secrets.yaml

generatorOptions:
  disableNameSuffixHash: true
