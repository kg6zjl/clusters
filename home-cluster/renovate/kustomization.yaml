apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: renovate

resources:
  - dns-egress.yaml
  - namespace.yaml
  - external-secrets.yaml

generatorOptions:
  disableNameSuffixHash: true
