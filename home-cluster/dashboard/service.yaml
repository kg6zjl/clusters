apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
  namespace: kube-system
spec:
  selector:
    k8s-app: kubernetes-dashboard
  ports:
    - port: 80        # The port Caddy or your browser hits
      targetPort: 80  # The port your container listens on
      protocol: TCP
