apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: meshmonitor-tcp-4405
  namespace: meshtastic
spec:
  entryPoints:
    - meshmonitor-4405
  routes:
    - match: HostSNI(`*`)
      services:
        - name: meshmonitor
          port: 4405
          terminationDelay: 0
