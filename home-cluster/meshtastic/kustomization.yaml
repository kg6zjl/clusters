apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - dns-egress.yaml
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml
  - dnsendpoint.yaml
  - external-secrets.yaml

generatorOptions:
  disableNameSuffixHash: true
