apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ai-services-allow-egress
  namespace: ai-services
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
    - protocol: TCP
      port: 443
  # Ollama + ComfyUI on GPU host (beast) - 192.168.1.161
  - to:
    - ipBlock:
        cidr: 192.168.1.161/32
    ports:
    - protocol: TCP
      port: 11434
    - protocol: TCP
      port: 8080
  - to:
    - namespaceSelector: {}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik
