apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: auth
  namespace: kube-system
spec:
  endpoints:
  - dnsName: auth.kube.stevearnett.com
    recordType: CNAME
    targets:
    - kube.stevearnett.com