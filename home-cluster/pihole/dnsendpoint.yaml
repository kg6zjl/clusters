apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: pihole
  namespace: kube-system
spec:
  endpoints:
  - dnsName: pihole.kube.stevearnett.com
    recordType: CNAME
    targets:
    - kube.stevearnett.com
